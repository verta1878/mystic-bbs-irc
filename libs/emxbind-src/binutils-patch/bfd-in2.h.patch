--- a/bfd/bfd-in2.h	2018-01-13 13:31:15.000000000 +0000
+++ b/bfd/bfd-in2.h	2026-07-09 10:05:45.988738754 +0000
@@ -6650,6 +6650,11 @@
      with this name and type in use.  BSF_OBJECT must also be set.  */
 #define BSF_GNU_UNIQUE          (1 << 23)
 
+  /* EMX (OS/2) a.out import symbols: N_IMP1/N_IMP2 external references that
+     emxbind turns into LX imports.  Ported from emx-patched binutils. */
+#define BSF_EMX_IMPORT1         (1 << 28)
+#define BSF_EMX_IMPORT2         (1 << 29)
+
   flagword flags;
 
   /* A pointer to the section to which this symbol is
